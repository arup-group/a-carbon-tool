import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // initiate data structure to be used
  },
  getters: {
    // home for computed functions we want to use to assess streams for carbon
  },
  mutations: {
    // any methods we want to run on the state data
    // use 'commit' to initiate within funtions

  },
  actions: {
    // asyncronous tasks
    // use dispatch to initiate within components / functions
    
  },
  modules: {},
});
